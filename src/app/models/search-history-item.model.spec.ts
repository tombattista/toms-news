import { SearchHistoryItem } from './search-history-item.model';

describe('SearchHistoryItem', () => {
  it('should create an instance', () => {
    expect(new SearchHistoryItem()).toBeTruthy();
  });
});
